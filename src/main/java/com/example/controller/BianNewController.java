package com.example.controller;import org.springframework.stereotype.Controller;import org.springframework.web.bind.annotation.*;import static com.example.bian.client.bushu.PrivateConfig.*;@RestController@CrossOrigin@Controllerpublic class BianNewController {    BizService bizService = new BizService();    @RequestMapping(value = {"/index"}, method = {RequestMethod.GET})    public String index() {        return "index";    }    @RequestMapping(value = {"/genDan"}, method = {RequestMethod.POST})    @ResponseBody    void genDan(@RequestBody String data) throws InterruptedException {        fangWenTiem = System.currentTimeMillis();//        PrivateConfig.printLog(fangWenTiem + "");        if(!stop){            bizService.doBiz(data);        }    }    public static void main(String[]  args){        BianNewController bianNewController = new BianNewController();        System.out.println();    }}